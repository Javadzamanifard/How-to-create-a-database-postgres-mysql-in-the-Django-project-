# پروژه جنگوی من — راه‌اندازی 

این پروژه با **Django** ساخته شده است.  
راهنمای زیر نشان می‌دهد چگونه محیط توسعه را در ویندوز (یا سایر سیستم‌عامل‌ها) راه‌اندازی کنید.

---

## 📋 پیش‌نیازها

- Python 3.8 یا بالاتر
- pip (مدیر بسته‌های پایتون)
- MySQL Server 8.0+, postgres 18
- (اختیاری) MySQL Workbench — برای مدیریت گرافیکی دیتابیس

---

## 🚀 مراحل راه‌اندازی

### 1. کلون کردن پروژه
```bash
git clone https://github.com/your-username/your-repo.git
cd your-repo
2. ایجاد و فعال‌سازی محیط مجازی
python -m venv venv
# فعال‌سازی در ویندوز:
venv\Scripts\activate
# فعال‌سازی در لینوکس/مک:
source venv/bin/activate
3. نصب وابستگی‌ها
pip install -r requirements.txt

 اگر requirements.txt وجود ندارد، ابتدا این دستور را اجرا کنید:
pip install django environs psycopg2-binary(or mysqlclient)
4.ب) ایجاد دیتابیس و کاربر اختصاصی
مطابق فایل DataBaseHelp.txt عمل کنید
5. تنظیم متغیرهای محیطی
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

DB_NAME=ban_db
DB_USER=ban_user
DB_PASSWORD=ban_password123
🔐 نکته امنیتی:

فایل .env را هرگز در گیت نگذارید (در .gitignore قرار دارد).
در محیط تولید (production)، از DEBUG=False و رمزهای پیچیده‌تر استفاده کنید

6. اجرای مایگریشن‌ها(ابتاد میتوانید مدل اختصاصی کاربر رو CustomUser را ایجاد کنید)
7. ایجاد کاربر سوپریوزر (اختیاری)
8. راه‌اندازی سرور


📁 ساختار پروژه
your-repo/
├── .env                 # ⚠️ در گیت کامیت نشود!
├── .gitignore           # شامل .env
├── manage.py
├── requirements.txt
└── your_project/
    ├── settings.py
    └── ...

📝 یادداشت‌های توسعه‌دهنده
همیشه از کاربر اختصاصی (نه root) برای اتصال به دیتابیس استفاده کنید.
برای پشتیبانی کامل از فارسی، حتماً از utf8mb4 استفاده شده است.
با تشکر از همکاری شما! 🙌
برای هرگونه سؤال، issue ایجاد کنید یا با توسعه‌دهنده تماس بگیرید.
email: javadzamanii.1374@gmail.com
