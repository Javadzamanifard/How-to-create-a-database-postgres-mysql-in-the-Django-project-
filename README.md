🌐 راه‌اندازی پروژه جنگو (Django Project Setup)

این مخزن شامل یک پروژه وب مبتنی بر Django است.
در این راهنما مراحل کامل نصب، راه‌اندازی و اجرای پروژه شرح داده شده است.

📋 پیش‌نیازها

برای اجرای پروژه به موارد زیر نیاز دارید:

Python 3.8+

pip – مدیر بسته‌های پایتون

MySQL Server 8+ یا PostgreSQL 13+

(اختیاری) MySQL Workbench / pgAdmin جهت مدیریت دیتابیس

🚀 مراحل نصب و راه‌اندازی
1) کلون کردن مخزن
git clone https://github.com/your-username/your-repo.git
cd your-repo

2) ایجاد و فعال‌سازی محیط مجازی
python -m venv venv

فعال‌سازی در ویندوز:
venv\Scripts\activate

فعال‌سازی در لینوکس/مک:
source venv/bin/activate

3) نصب وابستگی‌ها

اگر فایل requirements.txt موجود است:

pip install -r requirements.txt


اگر وجود ندارد ابتدا نصب کنید:

pip install django environs psycopg2-binary mysqlclient

4) ایجاد دیتابیس و کاربر اختصاصی

بخش مربوطه در فایل DataBaseHelp.txt قرار دارد.
حتماً برای اتصال Django یک کاربر جداگانه (نه root) بسازید.

5) ساخت فایل متغیرهای محیطی .env

یک فایل .env در ریشه پروژه بسازید:

SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

DB_NAME=ban_db
DB_USER=ban_user
DB_PASSWORD=ban_password123
DB_HOST=localhost
DB_PORT= mysql port or postgres port

🔐 نکات امنیتی

فایل .env نباید در Git کامیت شود. (در .gitignore قرار دارد)

در حالت Production مقدار DEBUG=False و رمزهای قوی استفاده شود.

از charset utf8mb4 برای پشتیبانی کامل از زبان فارسی استفاده کنید.

6) اجرای مایگریشن‌ها
python manage.py migrate


اگر از CustomUser استفاده می‌کنید، ابتدا مدل آن را بسازید سپس مایگریشن بگیرید:

python manage.py makemigrations
python manage.py migrate

7) ایجاد کاربر سوپریوزر (اختیاری)
python manage.py createsuperuser

8) اجرای سرور توسعه
python manage.py runserver

📁 ساختار پروژه
your-repo/
├── .env                 # ⚠️ در Git قرار نگیرد
├── .gitignore           # شامل .env
├── manage.py
├── requirements.txt
└── your_project/
    ├── settings.py
    ├── apps/
    ├── ...
    └── ...

📝 یادداشت‌های توسعه‌دهنده

همیشه از کاربر اختصاصی دیتابیس استفاده کنید.

در محیط Production از وب‌سرورهای مثل Nginx/Gunicorn استفاده کنید.

برای پشتیبانی از فارسی، دیتابیس را روی utf8mb4_general_ci تنظیم کنید.

در صورت بروز مشکل یا سؤال، لطفاً Issue ثبت کنید.

📬 اطلاعات تماس
javadzamanii.1374@gmail.com
